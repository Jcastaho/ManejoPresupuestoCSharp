@{
    ViewData["Title"] = "Reporte Excel";
    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var mesAnterior = haceUnMes.Month;
    var anioActual = hoy.Year;
    var anioAnterior = haceUnMes.Year;
}

<partial name="_SubMenu" model="SubMenuTransacciones.ExcelReporte" />


<h1>Exportar Transacciones a Excel</h1>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual"
       class="btn btn-success">

        Exportar mes actual
    </a>~@hoy.ToString("MMMM yyyy")
</div>
<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-anio="@anioActual"
       class="btn btn-success">

        Exportar mes anterior
    </a>~@haceUnMes.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual"
       class="btn btn-success">

        Exportar año actual
    </a>~@hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual"
       class="btn btn-success">

        Exportar año anterior
    </a>~@hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-anio="@anioActual"
       class="btn btn-success">

        Exportar todo
    </a>
</div>